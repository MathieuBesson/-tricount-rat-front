export enum Ressource {
    Users = "users",
    Expenditures = "expenditures",
    ExpenditureCategories = "expenditure-categories",
    ExpenditureUser = "expenditures/user"
}

export enum HttpMethod {
    Get = "GET",
    Post = "POST",
}
